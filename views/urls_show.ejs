<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include ./partials/_header %>
  <h2> Your URL </h2>
  <p><a href=' /u/<%= shortURL %>'> <%= shortURL %> - <%= longURL %> </a></p>   <!-- redirect to long url -->
  <p> Date created: <%= date %> </p>   <!-- date when original linked created -->
  <% for (let key in visitObj) { %>   <!-- loop unique visitors and date -->
  <ul>
    <li> <%= key %> visited on <%= visitObj[key] %> </li>
  </ul> <% }; %>
  <p> Number of Visits: <%= visits[0] %>, Unique Visits: <%= uniqueVisits %>  </p>
  <form method='POST' action='/urls/<%= shortURL %>'>   <!-- edit, new long url -->
    <label for='longURL'> URL: </label>
    <input type='url' id='longURL' name='newURL'  value='http://' />
    <button type='submit'> Edit URL </button>
  </form>
  <a href='/urls'> Back to index </a>
</body>
</html> 
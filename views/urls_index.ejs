<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
    <% include ./partials/_header %>
    <!-- Add EJS code here -->
    <% if (!user) { %>
    <h2> Please login </h2>
    <% } else { %>
    <a href='/urls/new'> New URL </a>
    <ul>
      <% for (let users in urls) { %>
      <% if (urls[users].userId === user.id) { %>
        <li><a href='#'> <%= users %> - <%= urls[users].longURL %> </a></li>
        <form method= "POST" action ="/urls/<%= users %>/delete">
          <input type='hidden' id='shortURL' name='shortURL' value = '<%= users %>' />
          <button type='submit'> delete </button>
        </form>
        <form method= "GET" action ="/urls/<%= users %>">
            <input type='hidden' id='shortURL' name='shortURL' value = '<%= users %>' />
            <button type='submit'> edit </button>
          </form>
      <% }}}; %>
    </ul>
    
    
</body>
</html>